import RegistrationForm from '#client/auth/RegistrationForm';

export default function Register() {
	return (
		<section className="flex w-full items-center justify-center">
			<RegistrationForm />
		</section>
	);
}
